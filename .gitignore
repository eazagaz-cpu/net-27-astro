# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production
.env.local
.dev.vars

# OS
.DS_Store
Thumbs.db

# editors
.idea/
.vscode/*
# Tracked so the wrangler credential isolation survives a fresh clone. Git
# cannot re-include a file whose parent directory is excluded, hence the /*.
!.vscode/settings.json
*.swp
*.swo

# scratch build artefacts
tmp/
tmp-*.log

# wrangler
.wrangler/

# this project's private Cloudflare login (see scripts/wrangler.mjs) — holds a
# refresh token, must never be committed
.cf-auth/

# credentials and service account secrets
net27-security*.json
*service_account*.json
*service-account*.json
*.key
*.pem

# session logs and chat logs (may contain credentials)
COMPLETE_CHAT_SESSION_LOG.md
SESSION_CHECKPOINT.md
PROJECT_SESSION_HANDOFF.md
net27-watch-antigravity-master-prompt.md


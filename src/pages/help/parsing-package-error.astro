---
/**
 * English help/parsing-package-error page. The body lives in HelpPage so this route and its
 * localized twins cannot drift apart.
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import HelpPage from '../../components/pages/HelpPage.astro';
import { DEFAULT_LANG } from '../../i18n/config';
---

<BaseLayout>
  <HelpPage doc="parsingPackageError" lang={DEFAULT_LANG} />
</BaseLayout>

---
/**
 * English search page. The body lives in SearchPage so this route and its localized
 * twins cannot drift apart.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import SearchPage from '../components/pages/SearchPage.astro';
import { DEFAULT_LANG } from '../i18n/config';
---

<BaseLayout>
  <SearchPage lang={DEFAULT_LANG} />
</BaseLayout>

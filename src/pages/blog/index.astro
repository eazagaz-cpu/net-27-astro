---
/**
 * English blog index. The body lives in BlogIndexPage so this route and its
 * localized twins cannot drift apart.
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import BlogIndexPage from '../../components/pages/BlogIndexPage.astro';
import { DEFAULT_LANG } from '../../i18n/config';
---

<BaseLayout>
  <BlogIndexPage lang={DEFAULT_LANG} />
</BaseLayout>
